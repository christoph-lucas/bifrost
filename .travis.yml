language: java

jdk:
  - oraclejdk8

script:
  - cd master
  - ./gradlew build
  - ./gradlew -Djava.net.preferIPv4Stack=true test

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
